# アーキテクチャ採用の理由 (Architecture Rationale)

本システム `kintai-sync` が、なぜ現在の構成（Cloud Run, Cloud Tasks, Firestore, Gemini API等）を採用しているのか、その技術的な背景と理由を解説します。

______________________________________________________________________

## 1. 処理の確実性と「Slack 3秒ルール」の回避

### 課題

SlackのWebhook（Event API）は、**3秒以内**にレスポンスを返さないと、タイムアウトとして処理を再送（リトライ）してしまいます。しかし、ジョブカンのスクレイピングやGemini APIの解析には3秒以上かかることが一般的です。

### 解決策：非同期分散処理（Cloud Tasks の採用）

- **仕組み**: 最初の受信機（Receiver）は「受け付けました」と3秒以内にSlackへ即答し、実際の重い処理は **Cloud Tasks** というキュー（待ち行列）に放り込みます。
- **理由**: これにより、Slack側でのタイムアウトを防ぎつつ、実際の処理はバックグラウンドで着実に実行できます。

______________________________________________________________________

## 2. 失敗に強いシステム（冪等性とチェックポイント）

### 課題

外部サービス（ジョブカンやGoogleカレンダー）への通信は、ネットワークエラーなどで時々失敗します。その際、単純に最初からやり直すと「ジョブカンの申請は終わっているのに、Slack通知でこけたから、リトライで二重に申請してしまう」という事故が起きます。

### 解決策：Firestore による状態管理

- **仕組み**: 各タスク（ジョブカン、カレンダー、Slack等）の完了状態を **Firestore** に記録します。
- **理由（なぜログ検索ではないのか）**:
  - **信頼性**: ログの検索は反映に時間がかかることがあり、リトライ時に「実は成功していた」ことを見逃すリスクがあります。
  - **一貫性**: Firestoreは書き込んだ直後に確実に読み取れる（強一貫性）ため、「どこまで成功したか」を正確に把握して、続きから再開できます。

______________________________________________________________________

## 3. 誰でも使える管理画面（運用の民主化）

### 課題

ユーザー設定（午前休の時間定義など）をコードやデータベース（Firestore）で直接管理すると、エンジニア以外が内容を修正できなくなります。

### 解決策：Google スプレッドシート ＋ Firestore への遅延読み込みキャッシュ

- **仕組み**: マスターデータは使い慣れた **スプレッドシート** で管理し、システムは実行速度を上げるためにそのコピー（キャッシュ）を **Firestore** から読み取ります。Workerは設定参照の直前にキャッシュの鮮度を確認し、`config.yaml` の `sync.cache_ttl_seconds` を超過している場合のみスプレッドシートを再読み込みしてFirestoreへ反映します（read-through キャッシュ）。
- **理由**:
  - **利便性**: 非エンジニアでもブラウザから簡単に設定変更が可能。
  - **速度**: 実行時に毎回スプレッドシートAPIを叩くと遅いため、Firestoreのキャッシュを参照して高速なレスポンスを実現します。
  - **同期方式の選定（なぜ GAS / Cloud Scheduler ではないのか）**:
    - 設定が変わるのはユーザー追加・変更時のみで稀であり、設定が参照されるのは勤怠投稿が処理される瞬間だけです。そのため「変化の有無に関わらず定期実行する」方式（Cloud Scheduler）や、外部スクリプトに依存する方式（GAS Webhook）、運用者が都度叩く手動コマンドは、いずれも過剰またはコスト・運用負荷を生みます。
    - 遅延読み込みなら、スプレッドシートへのアクセスはキャッシュミス時（実質 TTL ごとに最大1回）に限定され、専用の同期サービス・スケジューラ・SA を持たずに済み、構成が1つ簡素化されます。
  - **シートへのアクセス手段（なぜ SA 共有ではないのか）**:
    - スプレッドシートは自社（e-agency）管理下にありますが、Workspace の共有ポリシーがサービスアカウントのドメイン（`gserviceaccount.com`）を組織外として扱うため、SA への閲覧共有が許可されません。
    - そこで Worker は、シートを閲覧できるユーザーの **OAuth リフレッシュトークン**（Secret Manager 保管）を用いて「正規の利用者として」読み取ります。共有ポリシーにも管理者の追加設定にも依存せず、自己完結で動作します。OAuth 同意画面を Internal にすることでトークンは失効しません。

______________________________________________________________________

## 4. 高度な自然言語解析の容易化

### 課題

「明日の午前休」「来週月曜の全休」など、人によって書き方が異なるSlackメッセージを、正確にプログラムで解析するのは非常に困難です。

### 解決策：Vertex AI (Gemini API) の採用

- **理由**: 従来の正規表現（文字のパターンマッチング）では限界があった複雑な文章も、LLMを使うことで、極めて高い精度で「日付・種別・理由」を構造化データとして抽出できます。

______________________________________________________________________

## 5. コストの最小化と管理の自動化

### 課題

サーバーを立てると、使っていない時間も費用が発生し、管理の手間もかかります。

### 解決策：サーバーレス ＋ Terraform

- **理由**:
  - **サーバーレス (Cloud Run / Firestore)**: 今回の規模（1日15人程度）では、ほとんどのリソースが **無料枠** に収まり、月額費用をほぼ $0 に抑えられます。
  - **Terraform (IaC)**: インフラをコードで定義することで、誰の環境でも同じ構成を再現でき、不要になったら `terraform destroy` でボタン一つで全ての課金リソースを確実に消去できます。

______________________________________________________________________

## 6. スマートフィルタリングと「対象外」の処理

### 課題

Slackには勤怠連絡以外にも「家族は風邪を引いているが、自分は休みません」といった、勤怠に関する単語を含みつつも、実際の申請は不要なメッセージが投稿されることがあります。これらを誤って「休み」と判定すると、不要なジョブカン申請やカレンダー登録が発生してしまいます。

### 解決策：`attendance_type: "none"` の導入

- **仕組み**: LLM（Gemini）に対し、申請が不要なメッセージについては明示的に `none` という種別を返すように指示します。Worker側では、解析結果が `none` の場合に後続の連携処理（ジョブカン、カレンダー、ステータス更新等）をスキップし、Slackに「確認済み」の旨だけを返信して早期終了します。
- **理由**:
  - **精度の向上**: 「休み」という単語の有無ではなく、文章全体の文脈をLLMに判断させることで、誤動作を最小限に抑えます。
  - **効率性**: 不要な申請処理（Playwrightの起動など）を回避することで、リソース消費と実行時間を節約します。
  - **フィードバックの明確化**: ユーザーに対し「システムがメッセージを理解した上で、意図的に何もしなかった」ことを伝えることで、安心感を提供します。

______________________________________________________________________

## 7. 継続的な改善（Few-Shot 最適化）

### 課題

言語表現は多岐にわたるため、固定のプロンプト（指示文）だけでは、特定の方言や特殊な言い回しに対応できない可能性があります。

### 解決策：Vertex AI によるプロンプト最適化の統合

- **仕組み**: 期待される「メッセージと結果のペア」をCSV形式で管理し、Vertex AIの最適化ツール（Few-Shot Prompt Optimization）を使用して、最適なプロンプトを自動生成するワークフローを `Makefile` (`make prepare-test`) に組み込みました。
- **理由**: エンジニアが手動でプロンプトを試行錯誤するのではなく、データに基づいた体系的な精度向上を可能にします。

______________________________________________________________________

## 結論

このアーキテクチャは、\*\*「Slackの制約（3秒）を回避し、外部連携の失敗をFirestoreで確実にリカバーしつつ、非エンジニアがスプレッドシートで楽に管理できる」\*\*という、実運用での堅牢性と利便性を最優先に設計されています。
